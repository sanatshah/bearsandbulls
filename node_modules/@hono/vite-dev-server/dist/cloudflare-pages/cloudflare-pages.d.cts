import { WorkerOptions, MiniflareOptions } from 'miniflare';
import { GetEnv, Plugin } from '../types.cjs';

type Options = Partial<Omit<WorkerOptions, 'name' | 'script' | 'scriptPath' | 'modules' | 'modulesRoot' | 'modulesRules'> & Pick<MiniflareOptions, 'cachePersist' | 'd1Persist' | 'durableObjectsPersist' | 'kvPersist' | 'r2Persist'> & {
    assets?: boolean;
}>;
declare const getEnv: GetEnv<Options>;
declare const disposeMf: () => Promise<void>;
declare const plugin: (options?: Options) => Plugin;

export { plugin as default, disposeMf, getEnv };
