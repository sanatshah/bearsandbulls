import {} from '../../types.js';
import { deserializeJson } from '../../utils/deserializeJson.js';
import {} from '../types.js';
import { htmlToMetaTags } from './htmlToMetaTags.js';
export function htmlToContext(html) {
    const metaTags = htmlToMetaTags(html, 'meta[property^="frog:"]');
    const properties = {};
    for (const metaTag of metaTags) {
        const property = metaTag.getAttribute('property');
        if (!property)
            continue;
        const content = metaTag.getAttribute('content') ?? '';
        properties[property] = content;
    }
    return deserializeJson(properties['frog:context']);
}
//# sourceMappingURL=htmlToState.js.map